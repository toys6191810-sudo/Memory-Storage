# Memory Storage Windows Installer

Download `MemoryStorage-Windows-Installer-1.0.0.zip`, extract it, then run `Install.ps1`.

If Windows blocks the script, right-click `Install.ps1`, choose **Run with PowerShell**, and allow the installation prompt.

The package installs Memory Storage for Windows x64.
